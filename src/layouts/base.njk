<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}{% if title %} - {% endif%}{{ pkg.site.title }}</title>
  <meta name="description" content="{{ description if description else pkg.site.description }}">
  <link rel="stylesheet" href="{{ 'stylesheets/main.scss' | hashed }}">
</head>
<body>
  {{ content | safe }} 

  <script src="{{ 'javascripts/main.js' | hashed }}"></script>
</body>
</html>
